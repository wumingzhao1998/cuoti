# Vercel部署专用 - 轻量版依赖
# 移除了opencv-python和numpy以减小部署包大小

# 核心依赖
requests>=2.31.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# 工具库
pydantic>=2.0.0
typing-extensions>=4.8.0

# 注意：图像处理功能（去手写）在Vercel上需要外部服务或API
# 可以考虑使用：
# 1. 外部图像处理API（如阿里云、腾讯云等）
# 2. 单独的图像处理服务
# 3. 客户端处理后再上传

